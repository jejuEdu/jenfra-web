import * as S from './style.js';
const LoadingSpinner = () => {
  return (
    <S.LoadingSpinner>
      <img alt="loading spinner" src="/images/spinner.svg" />
    </S.LoadingSpinner>
  );
};

export default LoadingSpinner;
